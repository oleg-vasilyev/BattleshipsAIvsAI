namespace BattleshipsAIvsAI.Models
{
    enum PlayerNumber { First, Second, Unknown }
    enum CellCondition { Sinked, Hit, Miss, Unknown }
    enum ShipType { SingleDeck, DoubleDeck, TrippleDeck, QuadDeck }
    enum ShipCondition { Unbroken, Damaged, Sinked }
}
